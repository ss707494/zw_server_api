{
  "dependencies": {
    "typescript": "^3.8.3",
    "typeorm": "^0.2.24"
  },
  "scripts": {
    "tsc": "tsc",
    "log": "tsc && typeorm schema:log",
    "generate": "tsc && typeorm migration:generate -n UserMigration",
    "run": "tsc && typeorm migration:run"
  }
}
