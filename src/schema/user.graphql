type User {
    id: String
    name: String
    auth: String
    message: String
}

input UserSortInput {
    code: Int
}

input UserInput {
    id: String
    name: String
    auth: String
    message: String
    page: Int
    rowsPerPage: Int
    sortData: UserSortInput
}

input ChangePassword {
    id: String
    verification: String
    newPass: String
}
