
interface listSearch {
    page: Int
    rows_per_page: Int
    sort_type: String
}

type result {
    flag: Int
    msg: String
    category: Category
    product: Product
    shopCart: ShopCart
    address: Address
    payCard: PayCard
    order: Order
}

