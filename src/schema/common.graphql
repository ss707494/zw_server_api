
scalar JSON

fragment ProductFragment on Product {
    id
}

type result {
    flag: Int
    msg: String
    category: Category
    product: Product
    shopCart: ShopCart
    address: Address
    payCard: PayCard
    order: Order
    user: User
    dictionary: Dictionary
    dataConfig: DataConfig
    token: token
    promoCode: PromoCode
}

type token {
    token: String
    refreshtoken: String
}

