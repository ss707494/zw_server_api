
interface listSearch {
    page: Int
    rows_per_page: Int
    sort_type: String
}

fragment ProductFragment on Product {
    id
}

type result {
    flag: Int
    msg: String
    category: Category
    product: Product
    shopCart: ShopCart
    address: Address
    payCard: PayCard
    order: Order
    user: User
    dictionary: Dictionary
    token: token
}

type token {
    token: String
    refreshtoken: String
}

